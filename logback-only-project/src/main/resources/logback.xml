<configuration>
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>out/perf-secure.log</file>
        <append>false</append>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%msg%n MDC_SECURE_DATA=%X{SECURE_DATA}%n</pattern>
        </encoder>
    </appender>

    <appender name="SECURE_FILE" class="team.frog.securelogecc.logback.SecureMaskingAppender">
        <appender-ref ref="FILE"/>
    </appender>

    <appender name="ASYNC_SECURE_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>false</neverBlock>
        <includeCallerData>true</includeCallerData>
        <appender-ref ref="SECURE_FILE"/>
    </appender>

    <logger name="perf.file" level="info" additivity="false">
        <appender-ref ref="ASYNC_SECURE_FILE"/>
    </logger>

    <root level="info"/>
</configuration>
